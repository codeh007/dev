{
  "name": "GoMTM Dev Environment",
  "image": "docker.io/gitgit188/gomtm:v0.8.1771",
  "privileged": true,
  "remoteUser": "code",

  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "version": "latest",
      // Debian 12 (Bookworm) 默认支持 Moby，注释掉是对的
      // "moby": false, 
      "dockerDashComposeVersion": "v2"
    }
  },

  "hostRequirements": {
    "cpus": 4
  },

  // 🔴 修正 1：改为 true，告诉 VS Code 我们要覆盖默认的 CMD
  "overrideCommand": true,

  // 🔴 修正 2：显式指定一个“不死”的命令
  // docker-in-docker 的脚本会作为 entrypoint 运行，然后它会执行这里的 command
  "command": ["sleep", "infinity"],

  "customizations": {
    "vscode": {
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },

  // 🔴 修正 3：确保日志重定向正确，避免 permission denied 或找不到路径
  // 建议输出到 /tmp 或用户目录下
  "postStartCommand": "sudo nohup gomtm entrypoint --mtgate-url=https://mtgateapi.yuepa8.com > /tmp/gomtm-start.log 2>&1 &",

  "postAttachCommand": "gomtm upgrade && gomtm install --dev"
}
